{
  "name": "src",
  "version": "1.0.0",
  "description": "test",
  "author": "César Sipaque",
  "private": true,
  "scripts": {
    "dev": "cross-env NODE_ENV=development nodemon server/index.js --watch server",
    "build": "nuxt build",
    "start": "cross-env NODE_ENV=production node server/index.js",
    "generate": "nuxt generate"
  },
  "dependencies": {
    "bootstrap": "^4.1.3",
    "bootstrap-vue": "^2.0.0-rc.11",
    "cross-env": "^5.2.0",
    "express": "^4.16.4",
    "nuxt": "^2.0.0",
    "vue-router": "^3.0.7"
  },
  "devDependencies": {
    "nodemon": "^1.18.9"
  }
}
